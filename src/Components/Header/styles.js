import styled from 'styled-components';
import { Box } from '@material-ui/core';

export const BoxDiv = styled(Box) `
    display: flex;
    flex-flow: row wrap;
    align-items: flex-end;
    padding: .2em;

    background-color: #1a95cd;
    border-radius: 50px
`;